apiVersion: v1
kind: Service
metadata:
  name: concurrent-job-queue
spec:
  selector:
    app: concurrent-job-queue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
